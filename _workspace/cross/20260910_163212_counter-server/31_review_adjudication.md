# 31_review_adjudication — 리뷰 조정 (⚠ Claude 단독 / 저하 모드)

**중대 제약:** Codex 토큰 부족으로 `30_codex_review.md`·`31_codex_adjudication.md`가 없다(DEGRADED.md 참조). 본 조정은 **Claude 리뷰어(30_claude_review.md) 단독**이며 이종 모델 교차 검증이 아니다. 조정자=오케스트레이터(Claude). 상호 검증 부재를 완화하기 위해, 코드 수정을 유발하는 지적은 오케스트레이터가 근거를 직접 재확인한 뒤 채택한다.

## 판정

| # | 지적 | 심각도 | 판정 | 근거·조치 |
|---|---|---|---|---|
| R-C1 | 무응답 타임아웃 테스트가 워커 in-flight 경로를 실행 안 함 (`CounterEndToEndTests.cs:486`) | Low(품질) | **채택·수정** | 스크래치패드 프로브로 실증(Increment 0 도달=워커 미출발). 계획이 명시 요구한 검증이 실질 미이행 → 서버가 1회차 조회만 응답·이후 드롭하도록 테스트 수정 |
| R-C2 | 프레임 길이 교차검증 도달 불가 + Id=18 RequireBodySize 분기 미검증 (`CounterHandler.cs:98`) | Low | **채택·수정** | 트랜스포트 경유로는 unreachable → 핸들러 직접 호출 테스트 1개 추가로 방어 코드 자체를 검증 |
| R-C3 | `CounterHandler.Serializer` 필드 완전 미사용 + remarks 부정확 (`CounterHandler.cs:59`) | Low | **채택·수정** | 사문화된 필드 제거, 클래스 remarks의 "Deserialize에 넘기기 전에" 서술을 실제 동작에 맞게 교정 |
| R-C4 | 베이스 §5 "송신 타임아웃"의 블로킹 전제 미성립 | 문구 | **기각(결함 아님)** | 리뷰어 512MB 실측으로 반증 확인. 설계 문서 문구만 최종 보고에 정정 기록 |
| R-C5 | "실패 시 전체 시나리오 취소" 미구현 | Low(재현 미확인) | **부분 채택** | 대기자 실패는 구현됨. 전체 취소 부재는 무응답 테스트(R-C1) 수정으로 정리·불변식 주석 |
| R-C6 | 테스트 `stopTask` 이중 Stop() 이론적 가능 | Low(재현 미확인) | **채택·수정** | R-C1 테스트 개편 시 함께 정리(Stop 1회 보장) |
| R-C7 | 포기된 조회의 지연 응답이 새 대기자 가로챔 (도달 불가) | Low(재현 미확인) | **기각** | 현재 흐름 도달 불가. 연결당 미완료 조회 1개 불변식을 주석으로 명문화 |
| 취향 5건 | 명명·포트 등 | [취향] | **무조치** | 결함 아님 |

## 수정 대상 요약 (구현자 전달)
R-C1(테스트 실질화), R-C2(핸들러 직접 테스트 추가), R-C3(dead 필드 제거+주석 교정), R-C6(Stop 1회 보장), R-C7(불변식 주석). 전부 테스트/주석/dead code 범위 — 제품 동작 변경 없음.

## 미해결 (최종 보고 승계)
- **Phase 3 교차 검증 부재** — 위 판정은 Claude 단독. Codex 재개 후 재리뷰 시 뒤집힐 여지 있음.
- R-C1이 지적한 "배리어 필요성은 결정적으로 증명 불가"(flaky 없이는) 한계 — Phase 1 F-3과 동일, 설계 근거로만 성립.
